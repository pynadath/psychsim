We provide data for the two Predict instances for these requests. The request's specifications for the Explain instances are sufficiently different as to require a separate set of requests.

We provide the matching pre/post-survey participant IDs as in requests 14 & 15.

The tax is imposed at the start of the HoloCane timeline.

We interpreted requests for a 5-point scale as errors and used 7-point scales instead. If a 5-point scale was actually intended, we can provide the equivalent 5-point values in a revised data set.

Actors can answer questions about only those friends who were also participating in the experiment.
