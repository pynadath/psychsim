There are no instances specified, so we applied the request to the Phase 2 Predict instances.

There is no timeline specified, so we retroactively applied the instrument to the most recent hurricane in the IDP timeline.

There is no sampling strategy specified, so we asked a random sample of 10% of the population.